# -----------------------------
# NOM DU MICRO-SERVICE
# -----------------------------
spring.application.name=order-service

# -----------------------------
# PORT DU SERVICE
# -----------------------------
server.port=8091

# -----------------------------
# CONFIGURATION BASE DE DONNEES (MySQL)
# -----------------------------
spring.datasource.url=jdbc:mysql://localhost:3306/order_service?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

# -----------------------------
# CONFIGURATION JPA / HIBERNATE
# -----------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# -----------------------------
# EUREKA CLIENT CONFIGURATION
# -----------------------------
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# -----------------------------
# ACTUATOR (OPTIONNEL)
# -----------------------------
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# -----------------------------
# JWT CONFIGURATION
# -----------------------------
jwt.secret=your-very-secure-jwt-secret-key-that-is-at-least-256-bits-long-change-this-in-production
jwt.expiration=86400000

# Gateway base URL
gateway.url=http://localhost:8222